===
replace 'if (***) {***}' 'if (**1) { return (pass); }'
===
vcl 4.1;

sub vcl_recv {
    if ((req.url ~ "^/api/") || (req.url ~ "^/health/")) {
        set req.http.X-API = "true";
    }
}
===
vcl 4.1;

sub vcl_recv {
    if ((req.url ~ "^/api/") || (req.url ~ "^/health/")) {
        return (pass);
    }
}
