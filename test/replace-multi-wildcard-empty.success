===
replace 'acl purge {***}' 'acl purge {"127.0.0.1";}'
===
vcl 4.1;

acl purge {
}

sub vcl_recv {
    if (req.url ~ "^/api/") {
        set req.http.X-API = "true";
    }
}
===
vcl 4.1;

acl purge {
    "127.0.0.1";
}

sub vcl_recv {
    if (req.url ~ "^/api/") {
        set req.http.X-API = "true";
    }
}
